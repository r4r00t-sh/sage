/**
 * Product toggles. Set to true to re-enable features without a full refactor.
 */
export const CHAT_ENABLED = false;

/** AI assistant FAB + panel (uses POST /assistant/chat; requires GEMINI_API_KEY on server). */
export const ASSISTANT_ENABLED = true;
